--find-links ./vendor/wheels

# Web framework
Flask==3.0.0

# HTTP client for intranet image downloads
requests==2.31.0

# YOLO inference engine
ultralytics==8.4.27
torch==2.11.0
torchvision==0.26.0

# Local wheel for the Ultralytics CLIP dependency used by YOLOE / YOLO-World.
./vendor/wheels/clip-1.0-py3-none-any.whl

# Numerical / image processing
numpy==2.0.2
opencv-python-headless>=4.8,<5
Pillow==10.4.0
onnxruntime==1.22.1
onnx>=1.18,<2
tqdm==4.67.1
psycopg2-binary==2.9.10

# Oracle database driver
oracledb==3.4.0

# Offline installation notes:
# 1. Download all wheels into ./vendor/wheels first.
# 2. Then install with:
#    uv pip install --python .\.venv\Scripts\python.exe --offline -r requirements.txt
# 3. If you need the pinned dependency set, use:
#    uv pip sync --python .\.venv\Scripts\python.exe requirements.lock
