# multi-rider · AGENTS.md

> **上级约定**：本项目在 `~/project/` 下，`~/project/AGENTS.md` 的全部约定同样适用，开工前先读一遍。
> 尤其是第八条 —— 动手前先扫一遍 `~/project/skills/`，有相关 skill 就**完整读完再做**。

## 项目简介（2026-09-19 从 ~/.agents/skills 迁来）

下发/短信见 `province-task-dispatch` 与 `oracle-sms-gateway` 两个 skill。
检测和未成年人看板共用这一套服务，改推理参数时两边都会受影响。
